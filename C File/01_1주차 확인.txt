1. 자신의 이름을 gets() 함수로 입력받아 printf() 함수로 출력

2. 변수에 상수를 입력 후 출력 '=' 사용
   정수형(10진수, 8진수, 16진수로 출력)
   실수형(소숫점 2째 자리까지 출력) 
   1개의 문자(문자 및 10진수로 각각 출력)
   각각 변수의 주소값 출력(16진수로 출력)

3. 소수점 3자리 실수를 입력 받아 첫째, 둘째, 셋째 자리에서 각각 반올림하여 출력
   예) 소수점 3자리 실수 입력 : (12.345)
       출력 : 12, 12.3, 12.35

4. 화씨 온도를 입력한 후 섭씨온도로 출력(단, 소수 첫째 자리까지)
   참조) 섭씨온도 = 5 / 9 * (화씨온도 - 32 )

5. 세 개의 정수를 입력한 후 합계와 평균을 출력
   (단, 평균은 소수 둘째 자리에서 반올림)

6. 한 개의 영문자를 입력한 후 알파벳에서 몇 
   번째 문자인지 출력 
   출력예) 영문자 입력 : C
           결과 : C는 3번째 알파벳이다.

7. 두 개의 정수를 두 개의 변수에 입력한 후 내용을 서로 교환하여 출력
   출력예) 두 정수  : 1, 2
           교환결과 : 2, 1

9. 두 수를 입력 받아 덧셈 연산
   출력예) 정수 입력 : 3
           정수 입력 : 2
           두 정수의 합 : 5

10. 문자 하나를 입력받아 숫자로 변환하여 출력
     출력예) 문자입력 : 3
             숫    자 : 3

=========================================================
[연산자]

11. 출력결과?

	int a = -1, b = 20, c = 30;

	int x;

	x = ++a || ++b && ++c;

	printf("%d %d %d %d\n", x, a, b, c );		


12. 출력결과?


	int a = -1, b = 20, c = 30;

	int x;

	x = ++a || ++b || ++c;

	printf("%d %d %d %d\n", x, a, b, c );



13. 출력결과?

	int a = 5, b = 6, c = 5, d =1;
	int x; 

	x = a < b || a < c && c < d;

	printf("%d\n", x);


14. 출력결과?

	int x =2, y = 0, z = 2, k;

	k = ++x && ++y || ++z;

	printf("%d %d %d %d\n", x, y, z, k);	



15. 출력결과?

	int x = 1, y = 2, z = 3, k;
	k = ++x || ++y && ++z;
	printf("%d %d %d %d\n", k, x, y, z);	// 1 2 -1 3 



/********************************************************
부호 연산자 , 논리 부정 연산자 , 나머지 연산자, 시프트 연산자 분석
*********************************************************/

#include <stdio.h>

void foo();
void woo();
void goo();
void ooo();

void main()
{
	foo();
	woo();
	goo();
	ooo();
}

// 16 부호 연산자 
void foo()
{
	int i = -10;
	i     = +i;		 // i??

	i     = -10;
	i     = -i;		// ??
}

// 17 논리 부정 연산자 
void woo()
{
	short s = 10;
	printf("%d\n", !s);

	s = -1;
	printf("%d\n", !s);

	s = 0;
	printf("%d\n", !s);
}

//18   비트 연산자
//    10을 -10으로 변경하려면?
void goo()
{
	short s = 10;
	
	short s1 =  ~s;		// ?

	short s2 =  ~s + 1;	// ?
	
}

//19   시프트 연산자
// int형의 앞의 상위 8bit와 뒤의 하위 8bit를 바꾸는 프로그램		 
// 단 int는 16bit로 가정
void woo()
{
	int x = 10;
	char temp;

	temp = x >> 8 ;
	x = x << 8;

	x = x | (unsigned char)temp;	//

	printf("%d\n", x);
}

